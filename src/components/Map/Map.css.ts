import { style } from "@vanilla-extract/css";

export const locationPopupText = style({ fontWeight: "700" });

export const photoPopupImage = style({ objectFit: "cover" });

export const photoPopupImageLink = style({
  color: "blue",
  textDecorationLine: "underline",
  fontSize: "0.8em",
});
