import { Inter, Lato } from "@next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  weight: "700",
});

export const lato = Lato({
  subsets: ["latin"],
  weight: ["400", "700"],
});
